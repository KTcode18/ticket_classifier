# ticket_classifier
Classify customer complaint tickets into subcategories (admin, storage, access) to efficiently resolve them and reduce lead time.
The models used are naive bayes, logistic regression & linear support vector machine (SVM).
